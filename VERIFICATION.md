# Проверка — 2026-09-24 (ветка deepl-translate)

- Ветка создана от `73d310d`. Адаптер `DeepLTranslator` и базовые тесты восстановлены из `5913c13` и дополнены проверкой HTTP 200, единственного непустого перевода. Worker использует DeepL для текста, темы и комментария через прежний интерфейс Translator; логика регистрации, согласования, очереди и доставки не менялась.
- Конфигурация требует `TRANSLATOR_API_KEY` и `TRANSLATOR_API_URL` (только официальные HTTPS-адреса Free/Pro без пути). Модель и температура больше не нужны. Проверка URL одинакова при `check` и запуске адаптера. `.env` не менялся, сохранённые переводы в БД не пересматривались. В DeepL нет поля `finish_reason`; проверяется его собственный формат `translations`.
- `composer test`, PHP 8.5.9 / PHPUnit 11.5.56 — **192 теста, 1175 проверок, ошибок нет**. Проверены Free/Pro, языки RU/EN-GB/ES/FR, ошибки 403/429/456/5xx, таймауты, пустые/некорректные ответы, восстановление и исчерпание повторов через Worker для текста/темы/комментария. Реальные API не вызывались. zip/intl/fileinfo включены временно; Composer предупредил о владельце Git-каталога, завершился с кодом 0. `git diff --check` — успешно.

# Проверка — 2026-09-24 (временный сброс тестовой анкеты)

- Добавлена `/resetuser ID`: только суперадминистратор при `APP_ENV=test`; флаг проходит через Config → Kernel → Staff, по умолчанию отключён. В тестовом `/help` есть подсказка. Очистка анкеты, резерва контактов, отмена pending/failed-заданий и постановка уведомлений выполняются в транзакции update. ID и возрастающая ревизия сохраняются, завершённые доставки не удаляются. Для затронутых отменой доставок обновляется отчёт.
- Тесты проверяют повторное заполнение всех полей с выбором языка и новым одобрением, безопасность старой кнопки, повтор update, освобождение контактов, запрет сброса в production/для обычного админа/пользователя, сохранность другой анкеты, отмену старых сообщений/комментариев/доставок и невозможность повторить отменённый failed-комментарий.
- `composer test`, PHP 8.5.9, PHPUnit 11.5.56 — **166 тестов, 994 проверки, ошибок нет**. Расширения zip/intl/fileinfo включены временно через `PHP_INI_SCAN_DIR`. Composer сообщил о владельце Git-каталога, но тесты завершились с кодом 0. `git diff --check` — успешно. На живом боте не проверялось, окружение и контейнеры не менялись. Перед публикацией временную функцию следует удалить; уже выполняющийся внешний запрос сброс не останавливает.

# Проверка — 2026-09-24 (обновление отчёта после догоняющих доставок)

- `SqliteStore::addCatchup()` сбрасывает `reported` только при фактической вставке нового задания доставки Telegram или email. Приватный `job()` возвращает результат вставки; повторные задания, отклонённые уникальным индексом, не открывают отчёт заново. После завершения новых доставок одобривший суперадминистратор получает обновлённый общий отчёт.
- `DeliveryTest` проверяет обновление суммарных счётчиков после первоначального отчёта, ожидание обоих каналов, отсутствие дублей при повторном вызове (включая другой язык), единый отчёт при добавлении пользователя до первоначального отчёта, ошибку догоняющего email и успешный повтор без повторной Telegram-доставки.
- `composer test`, PHP 8.5.9 (Windows CLI), PHPUnit 11.5.56 — **160 тестов, 950 проверок, ошибок нет**. zip/intl/fileinfo включены временно через `PHP_INI_SCAN_DIR`; код завершения 0, несмотря на предупреждение Composer о владельце Git-каталога. `git diff --check` — успешно. Реальные API не вызывались; ранее пропущенные отчёты автоматически не восстанавливаются.

# Проверка — 2026-09-24 (защита от незавершённых переводов)

- `OpenAiTranslator` принимает текст только при `finish_reason="stop"`. Иные значения, включая `length`, отсутствующий/null или некорректный признак завершения дают безопасный `ApiFailure` с разрешённым повтором. Ответ провайдера не включается в исключение. Провайдеры без `finish_reason` теперь отклоняются.
- Тесты адаптера проверяют 10 вариантов незавершённого/некорректного ответа. `TranslationCompletionTest` проверяет реальный адаптер с фейковым HTTP и SQLite в памяти для текста, темы и комментария: отсутствие сохранения/доставки обрывка, задержку повтора, восстановление после успешного ответа, остановку после 5 попыток и уведомление сотруднику, отсутствие исходного текста/обрывка/ключа в событиях логирования.
- `composer test`, PHP 8.5.9 (Windows CLI), PHPUnit 11.5.56 — **158 тестов, 915 проверок, ошибок нет**. zip/intl/fileinfo включены временно через `PHP_INI_SCAN_DIR`; код завершения 0, несмотря на предупреждение Composer о владельце Git-каталога. `git diff --check` — успешно. Реальные API не вызывались; существующие сохранённые переводы не пересматривались.

# Проверка — 2026-09-24 (задача 3: отчёты только одобрившему суперадмину)

- `composer test`, PHP 8.5.9 (Windows CLI), PHPUnit 11.5.56 — **142 теста, 759 проверок, ошибок нет**. Расширения zip/intl/fileinfo включены временно через `PHP_INI_SCAN_DIR`. Предупреждение Composer о владельце Git-каталога не помешало тестам; код завершения 0.
- Проверены точные получатели отчётов и окончательных ошибок перевода текста/темы: обычный автор, самоодобрение и другой суперадмин-автор. Проверены короткое уведомление автору, отсутствие дублей после повторного одобрения, повтор своей рассылки без деталей для автора, запрет повтора чужой и отчёт при сбое только email.
- `git diff --check` — успешно. Вызовы логирования не изменены, новые тексты не передаются в логи. Ручная проверка на живом боте не выполнялась; ранее поставленные задания уведомлений не переписывались.

# Проверка — 2026-09-24 (задача 1: подпись автора черновика)

- `composer test`, PHP 8.5.9 (Windows CLI), PHPUnit 11.5.56 — **133 теста, 628 проверок, ошибок нет**. Расширения zip/intl/fileinfo включены через временный `PHP_INI_SCAN_DIR`, без изменения системного php.ini. Composer сообщил о владельце Git-каталога; запуск тестов завершился с кодом 0.
- Добавлены 5 сценариев доставки подписи суперадмину через `Worker::tick()`: username, полное имя, только имя, ID и ошибка API. Проверены приоритет username, источник `callback_query.from`, защита от повторного update и отсутствие профиля/текста сообщения в `Log` при успехе и ошибке.
- `git diff --check` — успешно. На живом боте не проверялось.

# Проверка поставки — 2026-09-21 (итерация 5: перевод через OpenAI-совместимый API вместо DeepL)

- PHP 8.5.9 (Windows CLI): PHPUnit 11.5.56 — **124 теста, 514 проверок, ошибок нет**.
- Итерация 5: DeepL заменён на `OpenAiTranslator` — перевод через любой OpenAI-совместимый `chat/completions` API (DeepSeek, Moonshot Kimi, OpenRouter и т.п.). Конфигурация: `TRANSLATOR_API_KEY`, `TRANSLATOR_API_URL` (любой HTTPS-хост без встроенных доступов), `TRANSLATOR_MODEL`; опциональная `TRANSLATOR_TEMPERATURE` (0–2, по умолчанию не отправляется — части провайдеров, например Kimi k2.6+, разрешена только их температура). Очередь, одобрения, почта, Excel и регистрация не затронуты — перевод по-прежнему отрезан интерфейсом `Translator`. Живая проверка: Moonshot Kimi (`kimi-k2.6`) — переводы RU→ES/EN-GB успешны.

# Проверка поставки — 2026-09-21 (итерация 4: отписка пользователей)

- PHP 8.5.9 (Windows CLI): PHPUnit 11.5.56 — **121 тест, 509 проверок, ошибок нет**.
- Итерация 4: команда `/unsubscribe ID` (только суперадминистратор) переводит одобренного пользователя в статус `unsubscribed` — прекращаются Telegram и почтовая доставка, пользователь исключается из новых рассылок; повторная подача заявки возможна и снова требует одобрения. Команда добавлена в меню Telegram суперадминистратора.
- Установка меню (`commands`) стала устойчивой к кратковременным ограничениям Telegram: временные ошибки повторяются до трёх раз с паузой (`CommandMenu` переведён на интерфейс `TelegramGateway`, покрыт тестами `CommandMenuTest`).

# Проверка поставки — 2026-09-16 (доработка: согласование заявок и рассылок, email и Excel; итерация 3)

- PHP 8.5.9 (Windows CLI): PHPUnit 11.5.56 — **118 тестов, 484 проверки, ошибок нет**.
- Docker-образ `test` (PHP 8.4.25): тот же набор тестов — успешно; собран и рабочий образ `production`.
- Синтаксис всех PHP-файлов приложения, тестов и консольной команды проверен.
- Консольный цикл на отдельной пустой базе: `help`, `check`, `migrate`, `worker --once`, `backup`, `restore --confirm` — успешно, без реальных API-вызовов.
- Обновление базы первой версии: миграция `002_approvals_and_email.sql` применяется поверх `001_initial.sql`; старые завершённые аккаунты переводятся в черновики без молчаливого одобрения.
- Итерация 2 (ревью): очередь перевода темы упорядочена после перевода текста; доставки догоняющих отправок входят в отчёт; защита от неизвестных типов заданий; двухфазная доставка комментариев; `/retryjob` для неуспешных комментариев и Excel; команды консоли `check` и `commands` (`CommandMenu`); `APP_ENV` с разрешением незашифрованного SMTP только в `test`; строгие проверки почтовых настроек.
- Итерация 3 (живой запуск): исправлено падение worker на email-заданиях — удалено несуществующее в PHPMailer 6.12 свойство `$Timelimit` (deprecation динамического свойства на PHP 8.2+); добавлен регрессионный тест `SmtpMailerTest`; статус ошибки очищается при успешном завершении задания. После исправления оба контейнера работают стабильно, очередь Telegram/email/export обрабатывается; письма уходят на тестовый SMTP.

Не выполнялись: реальные вызовы Telegram/DeepSeek/SMTP, проверка писем на тестовом SMTP-приёмнике (Mailpit поднимается через `compose.test.yaml`, либо используется Mailtrap из `.env`), Linux SIGTERM-проверка в контейнере, развёртывание на сервере. Порядок этих проверок приведён в README. Реальная почтовая доставка подтверждается после заполнения SMTP-настроек.
